const LoginForm = () => {
  return (
    <div>
      <form>
        Username: <input type="text" />
        Password: <input type="password" />
        <button>login</button>
        <button>register</button>
      </form>
    </div>
  )
}

export default LoginForm
